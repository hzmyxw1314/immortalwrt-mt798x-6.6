name: Build AX6000 237

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Prepare environment
        run: |
          sudo apt update
          sudo apt install -y build-essential libncurses5-dev libssl-dev \
            python3 python3-pip python3-setuptools git wget unzip \
            libelf-dev libz-dev ccache rsync subversion swig time

      - name: Configure
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

          make defconfig

          # 基础必备插件
          echo "CONFIG_PACKAGE_luci-app-ttyd=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-upnp=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-wol=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-nlbwmon=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-filetransfer=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-store=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-sqm=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-ksmbd=y" >> .config
          echo "CONFIG_PACKAGE_luci-theme-argon=y" >> .config

          # WireGuard
          echo "CONFIG_PACKAGE_luci-app-wireguard=y" >> .config
          echo "CONFIG_PACKAGE_wireguard-tools=y" >> .config

          # OpenClash
          echo "CONFIG_PACKAGE_luci-app-openclash=y" >> .config

          # 打印服务
          echo "CONFIG_PACKAGE_luci-app-p910nd=y" >> .config
          echo "CONFIG_PACKAGE_p910nd=y" >> .config

          make defconfig

      - name: Compile firmware
        run: |
          make -j$(nproc)

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: immortalwrt-ax6000
          path: bin/targets/mediatek/mt7986/*.bin
